[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python -m src.app.main --mode scan --iterations 0"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Health check (optional)
healthcheckPath = "/"
healthcheckTimeout = 100


